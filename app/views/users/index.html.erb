<%= cloudinary_js_config %>
<p id="notice"><%= notice %></p>
<h1>Listing Users</h1>
<table>
  <% @users.each do |user| %>
	<tr class="inline-list">
		<% if user.image.present? %>
  			<td><%= cl_image_tag user.image_url, :class=>"image round-image" %></td>
  		<% else %>
  			<td><%= image_tag "profile.png", :class=>"image round-image" %></td>
  		<% end %>
    	<td><%= link_to user.profile_name, user %></td>
	</tr>
  <% end %>
</table>
